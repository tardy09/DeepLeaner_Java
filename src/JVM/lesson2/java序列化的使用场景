1.当你想把内存里面的一个对象保存到文件或者数据库里面进行持久化保存，在需要的时候再拿出来使用
2.当需要通过socket来网络上传输对象的时候
3.当需要rmi传输对象的时候


ps;
  1.序列化的时候，只需要对对象的状态进行保存，而不管对象的方法
  2.当一个父类实现序列化的时，子类自动实现序列化，不需要显示的实现序列化接口
  3.当一个类的实例变量引用其他对象，序列化对象时候也对引用对象进行了序列化操作
  4.