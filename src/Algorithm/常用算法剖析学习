1.排序算法
内部排序和外部排序算法
如果整个排序过程不需要借助外部存储器（如磁盘），所有排序操作都在内存里面完成
外部排序:数据量比较大，计算机无法将整个排序过程在内存里面完成，必须借助于外部存储器，这种排序称之为
外部排序--例如：多路归并排序，也就是将源文件分解成多个能多个能够一次性装入内存的
部分，分别把每一部分调入内存完成排序，接下来再对多个有序的子文件进行归并排序。
 常用的排序算法：选择排序--直接选择排序，堆排序
              交换排序--冒泡排序，快速排序
              插入排序---直接插入排序，折半插入排序，shell排序
              归并排序
              桶排序  --不常用
              基数排序 --不常用
